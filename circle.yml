machine:
    timezone:
        America/Denver
    python:
        version:
            3.5.1
dependencies:
    pre:
        - pip install pelican typogrify
deployment:
    publication:
        branch: master
        commands:
            - make html
            - cp -r output/* $CIRCLE_ARTIFACTS