machine:
    timezone:
        America/Denver
    python:
        version:
            3.5.1
dependencies:
    pre:
        - pip install pelican typogrify fabric
deployment:
    publication:
        branch: master
        commands:
            - fab build
            - cp -r output/* $CIRCLE_ARTIFACTS