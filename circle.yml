machine:
    timezone:
        America/Denver
    python:
        version:
            3.5.1
dependencies:
    pre:
        - pip install pelican typogrify awscli
deployment:
    publication:
        branch: master
        commands:
            - make publish ${CIRCLE_ARTIFACTS}
            - make s3_upload